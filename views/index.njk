
{% extends "base.njk" %}

{% block content%}
        {% for food in  menu %}
        <div class="card mt-4" >
            <div class="flex" >
                <h2>  {{ food.name }}   </h2>
                <form method="GET" >
                    <input type="submit" formaction="/delete/<%=food.id%>" class="love" value="Borrar">
                </form>
            </div>
            <p> {{ food.description }} </p>
            <a href="" > {{ food.link }} </a>
        </div>
    {% endfor %}
{% endblock %}

<!--





-->