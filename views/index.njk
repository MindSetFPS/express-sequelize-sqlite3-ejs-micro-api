
{% extends "base.njk" %}

{% block content%}
        {% for food in  menu %}
        <div class="card mt-4" >
            <div class="flex" >
                <h2>  {{ food.name }}   </h2>
                <div class="flex" >
                    <form method="GET" class="mx-1" >
                        <input type="submit" formaction="/edit/{{food.id}}" class="blue" value="Editar">
                    </form>
                    <form method="GET" class="mx-1" >
                        <input type="submit" formaction="/delete/{{food.id}}" class="love" value="Borrar">
                    </form>
                </div>
            </div>
            <p> {{ food.description }} </p>
            <a href="" > {{ food.link }} </a>
        </div>
    {% endfor %}
{% endblock %}

<!--





-->